# TODO / Идеи для улучшения

Список того, что можно доработать. Отсортировано по областям; `[x]` — уже сделано, `[ ]` — открыто.
Внутри каждого блока — примерно по убыванию пользы.

## Функциональность компонента

- [ ] **Элементы переменной высоты.** Сейчас `itemHeight` фиксированный. Динамическое измерение (через `ResizeObserver` / кэш высот) — главное функциональное ограничение относительно `react-window` / `@tanstack/virtual`.
- [ ] **Авто-расчёт `visibleItemsCount` от высоты контейнера.** Сейчас число видимых задаётся вручную; можно измерять контейнер (`ResizeObserver`) и считать окно само.
- [ ] **Убрать дублирующий проп `itemsCount`.** Его почти всегда можно вывести из `items.length`; оставить `itemsCount` только как опциональный override для «ленивых»/разреженных источников.
- [ ] **Горизонтальная виртуализация** (сейчас только вертикаль).
- [ ] **`overscan` вместо `paddingItemsCount`** — привести терминологию к общепринятой в экосистеме.
- [ ] **Колбэки** `onVisibleRangeChange` / `onScroll` наружу — чтобы потребитель мог, например, подгружать данные (infinite scroll).
- [ ] **`scrollToIndex(index)`** как императивный метод (через `ref` / `useImperativeHandle`).
- [ ] **a11y**: роли `role="list"` / `role="listitem"`, `aria-*`, поддержка клавиатурной навигации.

## Тесты

- [x] Юнит-тесты логики выбора индексов (Vitest + RTL), покрытие 100%.
- [ ] **Playwright** — реальный браузерный тест скролла/offset'ов + визуальная регрессия (скриншот-диффы) против собранного Ladle. Единственный слой матрицы, которого ещё нет; для виртуализации оправдан сильнее обычного (jsdom не считает layout).
- [ ] **Тест производительности** большого списка (100k) — что в DOM реально мало нод.
- [ ] Порог покрытия в `vitest.config.ts` (`coverage.thresholds`), чтобы CI падал при регрессе покрытия.

## Сборка и пакет

- [x] Сборка на tsup (ESM + `.d.ts`), корректные `exports`/`types`, `sideEffects: false`, peer-range `>=18`.
- [ ] **CJS-сборка** (`format: ["esm", "cjs"]`) — если нужна поддержка старых CJS-потребителей (`require`). Уберёт единственное ⚠️ у `attw` (node16-from-CJS).
- [ ] **`publint` + `attw` в CI** — шаг `npx publint && npx @arethetypeswrong/cli --pack`, чтобы манифест/типы не сломались незаметно.
- [ ] **Минифицированный бандл** (`minify: true`) — опционально, если важен вес.

## CI/CD и релизы

- [x] CI: `install → typecheck → lint → test → build`.
- [x] Деплой Ladle на GitHub Pages.
- [ ] **Changesets** (`@changesets/cli`) — версионирование по semver + генерация CHANGELOG + автопубликация. Заменит ручной бамп версии.
- [ ] **Автопубликация в npm по тегу** — workflow на `git tag v*` с `NPM_TOKEN`.
- [ ] **CHANGELOG.md** (ведётся Changesets автоматически).
- [ ] Матрица Node в CI (18/20/22) — если нужна совместимость со старыми Node.

## Качество кода / DX

- [x] husky + lint-staged (pre-commit: `eslint --fix` на staged).
- [x] `.nvmrc`.
- [ ] **Prettier** + `eslint-config-prettier` — единый автоформат, снять споры о стиле. Добавить в lint-staged.
- [ ] **Dependabot / Renovate** — авто-обновление зависимостей.
- [ ] **Coverage-бейдж честный** (Codecov или свой gist-бейдж) — сейчас статичный «100%», обновляется вручную.

## Документация

- [x] README (бейджи, API, демо-ссылка) + ARCHITECTURE.md.
- [ ] **Оглавление** в README.
- [ ] **GIF/скриншот** компонента в действии.
- [ ] **Секция сравнения** с `react-window` / `@tanstack/virtual` — честно очертить нишу («простые списки фиксированной высоты»).
- [ ] **CONTRIBUTING.md** — как контрибьютить (после Changesets особенно).
- [ ] JSDoc над `VirtualScroller` и пропсами — подсказки прямо в IDE у потребителя.
